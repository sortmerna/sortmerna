language: c
compiler:
  - clang
  - gcc
os:
  - linux
  - osx
sudo: false
install:
  - pip install numpy
  - pip install scipy
  - pip install python==2.7
  - pip install scikit-bio==0.2.3
script:
  - bash ./build.sh
  - cd tests; python test_sortmerna.py
